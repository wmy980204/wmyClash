# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 0'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      - name: Fetch Clash config
        run: wget --header="User-Agent:Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3" -O clash.yaml "https://dash.xn--cp3a08l.com/api/v1/pq/2a72e9980d4d446885fe314c730f6bc4"
        
      - name: Fetch secondClash config
        run: wget --header="User-Agent:Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3" -O secondClash.yaml "https://mysheep.dpdns.org/sub?token=4b8d1ecac329908ed8749999b6f493f0&clash"
        
      # - name: Check Clash config
      #   run: cat clash.yaml

      - name: Commit and Push changes
        env:
          TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config --global user.name "wmy980204"
          git config --global user.email "wmy8928@gmail.com"
          git pull origin main
          git add clash.yaml
          git add secondClash.yaml
          git commit -m "Update Clash config on $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
